CREATE TABLE Sets (
    id INTEGER PRIMARY KEY AUTOINCREMENT NOT NULL,
    reps INTEGER NOT NULL,
    weight INTEGER NOT NULL,
    exercise INTEGER NOT NULL,
    workout INTEGER NOT NULL
);

CREATE UNIQUE INDEX SET_UUID ON Sets(id);
